# ABOUTME: Example environment file showing required API keys and credentials
# ABOUTME: Copy this to .env and fill in your actual values

# AI provider (single canonical key; Gemini is the current provider)
AI_KEY=your_ai_provider_key_here
# Optional specialist override; defaults to google_genai:gemini-3.1-flash-lite-preview
# TOOL_MODEL=google_genai:gemini-3.1-flash-lite-preview

# Runtime V2 local mode (SQLite + local files; one daemon on one host)
# RUNTIME_V2_ENABLED=false
# RUNTIME_V2_URL=http://127.0.0.1:8700
# RUNTIME_STORAGE_BACKEND=sqlite
# RUNTIME_SQLITE_PATH=.data/runtime_v2.sqlite3
# RUNTIME_ARTIFACT_DIRECTORY=.data/runtime_artifacts
#
# Runtime V2 portable mode (PostgreSQL + S3-compatible object storage)
# RUNTIME_STORAGE_BACKEND=postgres
# DATABASE_URL=postgresql://user:password@host:5432/heathcliff?sslmode=require
# RUNTIME_INSTANCE_ID=heathcliff-rpi
# RUNTIME_STATE_ENCRYPTION_KEY=base64_urlsafe_32_byte_key
# RUNTIME_S3_BUCKET=heathcliff-artifacts
# RUNTIME_S3_ENDPOINT=https://s3.example.com

# Google Custom Search
GOOGLE_CSE_API_KEY=your_google_cse_api_key
GOOGLE_CSE_ID=your_custom_search_engine_id

# Tavily web search and clean webpage extraction (optional)
TAVILY_API_KEY=your_tavily_api_key

# Google APIs (Gmail, Calendar, Drive)
GOOGLE_APPLICATION_CREDENTIALS=path/to/credentials.json

# Spotify
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# Weather
OPENWEATHERMAP_API_KEY=your_openweathermap_key

# News
NEWSAPI_KEY=your_newsapi_key

# Twitter/X (Optional)
TWITTER_API_KEY=your_twitter_api_key
TWITTER_API_SECRET=your_twitter_api_secret
TWITTER_ACCESS_TOKEN=your_twitter_access_token
TWITTER_ACCESS_SECRET=your_twitter_access_secret

# Logging Configuration
# Set to "true" to enable file logging to logs/debug.log
ENABLE_FILE_LOGGING=false
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Langfuse Observability (optional but recommended)
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key
LANGFUSE_SECRET_KEY=your_langfuse_secret_key
# Override host if self-hosting Langfuse, or leave blank for SaaS
LANGFUSE_HOST= # e.g. https://cloud.langfuse.com or https://us.cloud.langfuse.com
# Optional release identifier shown in Langfuse UI
LANGFUSE_RELEASE=local-dev
# Process-level environment used for every v4 observation
LANGFUSE_ENVIRONMENT=local-dev
# Optional instrumentation version propagated to every observation
LANGFUSE_VERSION=

# Chroma Vector Database Configuration
CHROMA_API_KEY=your_chroma_api_key_here
CHROMA_DATABASE=heathcliff
CHROMA_TENANT=your_chroma_tenant_here

# Recent Context Store (defaults shown — override as needed)
# RECENT_CONTEXT_TTL_SECONDS=7200
# RECENT_CONTEXT_MAX_ITEMS=100
# RECENT_CONTEXT_MAX_SNIPPET_CHARS=1200
# RECENT_CONTEXT_MAX_RETURN=5
# RECENT_CONTEXT_STORE_PATH=temp/recent_memory.json

# Mem0 SDK Configuration
MEM0_USER_ID=heathcliff
